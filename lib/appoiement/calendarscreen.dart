import 'package:flutter/material.dart';

class appoiementscreen extends StatelessWidget {
  const appoiementscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}